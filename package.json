{
	"devDependencies": {
		"@electron/typescript-definitions": "^8.15.0",
		"@types/jest": "^29.5.5",
		"concurrently": "^8.2.1",
		"electron": "^27.0.1",
		"esbuild": "0.19.3",
		"jest-ts-webcompat-resolver": "^1.0.0",
		"ts-jest": "^29.1.1",
		"tsup": "^7.2.0",
		"typedoc": "^0.25.1"
	},
	"scripts": {
		"dev": "tsup main.ts src/engine/modules/bootstrap.ts --splitting --sourcemap --clean --treeshake --minify --metafile --watch",
		"build": "tsup main.ts src/engine/modules/bootstrap.ts --splitting --clean --treeshake --minify",
		"start": "electron .",
		"test": "jest"
	},
	"name": "reignitetypescript",
	"version": "1.96.1",
	"description": "A Godot/Unity-like simplistic 2d with basic 3d support game engine for the web and as a standalone desktop app.",
	"author": "Rei",
	"license": "MIT",
	"main": "dist/main.js"
}
